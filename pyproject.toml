[tool.black]
line-length = 88

[tool.isort]
profile = "black"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q"
testpaths = [
    "tests",
]

[tool.pylint.master]
ignore = [".venv", ".pytest_cache", ".git", "__pycache__"]
