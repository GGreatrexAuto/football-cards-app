# eslint-plugin-jsx-a11y and eslint-plugin-react declare peer support only up to
# ESLint v9 but work correctly with v10 via @eslint/compat fixupPluginRules.
# Remove this once both plugins publish ESLint v10 peer declarations.
legacy-peer-deps=true
